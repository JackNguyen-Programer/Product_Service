@model List<BanHang.ServiceSaveDate.save_date>
@if (@Model.Count <= 0)
{
    <p>Đang áp dụng</p>
}
else
{
    foreach (var item in Model)
    {

        <p>
            @item.date_start - @item.date_end
        </p>
    }
}
